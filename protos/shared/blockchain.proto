syntax = "proto3";

package sh.coherent.blockchain.shared;
option go_package = "github.com/coherentopensource/evm-etl/protos/go/shared";

enum Blockchain {
  ETHEREUM = 0;
  OPTIMISM = 1;
  POLYGON  = 2;
  AVALANCHE = 3;
  ARBITRUM = 4;
  GOERLI = 5;
}
